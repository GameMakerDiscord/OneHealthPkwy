/// @description Drop Panel
dragState = DragState.None;
if (iBuyHireWindow.dragging != id)
			return;

// Revert panel back to its spot
x = panelStartX;
y = panelStartY;
depth = panelDepth;

iBuyHireWindow.dragging = noone;